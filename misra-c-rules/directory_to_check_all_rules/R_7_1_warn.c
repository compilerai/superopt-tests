/* Octal constants shall not be used*/

#include <stdio.h>

int main(){
    int x = 011; // this is an octal constant
    // an octal constant is a constant that starts with 0 and is followed by a sequence of octal digits
    return 0;
}